[project]
name = "Audio_Summary"
version = "0.1.0"
description = "A simple app that uses LLMs to summarize audio conversations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
    "google-genai>=2.8.0",
    "python-dotenv>=1.2.2",
]
