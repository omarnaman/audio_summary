[project]
name = "asr-service-macos"
version = "0.1.0"
description = "mlx-whisper based transcription + diarization microservice for audio_summary (native macOS/Apple Silicon reference implementation)"
requires-python = ">=3.10,<3.14"
dependencies = [
    "asr-common",
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "mlx-whisper>=0.4",
]

[tool.uv.sources]
asr-common = { path = "../common" }
