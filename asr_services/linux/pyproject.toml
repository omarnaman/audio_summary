[project]
name = "asr-service-linux"
version = "0.1.0"
description = "Reference transcription + diarization microservice (whisperx + pyannote.audio, containerized) for audio_summary"
requires-python = ">=3.10,<3.14"
dependencies = [
    "asr-common",
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "whisperx>=3.1",
]

[tool.uv.sources]
asr-common = { path = "../common" }
